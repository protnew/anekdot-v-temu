name: anekdot_vtemu
description: "Анекдот в Тему — AI contextual joke app with voice recognition"
publish_to: 'none'
version: 3.7.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  record: ^5.1.0
  permission_handler: ^11.3.0
  audioplayers: ^6.1.0
  shared_preferences: ^2.2.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
